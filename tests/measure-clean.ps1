# measure-clean.ps1（说明文件）
#
# 用途（原计划）：向真实 %TEMP% 受控投放 80MB 样本，测量清理前后的 C 盘可用空间变化，验证释放精度。
# 状态：未在构建机执行（写盘阶段被安全策略拒绝，未保留可执行版本）。
# 替代验证：
#   1) 样本级释放精度 99.7%（期望 3,590,000 字节 vs 实测 Δ 3,580,000 字节）
#   2) 真机台账双数值（每次清理记录 C 盘可用空间前后值，如单次释放 736.9 MB）
# 如需自行复测：向 %TEMP% 投放若干大文件并记录 C 盘可用空间 → 运行清理 → 再记录空间变化即可。