<UserControl x:Class="CleanMaster.App.Views.CleanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Microsoft YaHei UI, Segoe UI" FontSize="13" Background="Transparent">
  <Grid Margin="12,8,12,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,8">
      <StackPanel Orientation="Horizontal">
        <Button x:Name="BtnScan" Content="扫描可清理项" Style="{StaticResource PrimaryButton}" Width="134"
                AutomationProperties.AutomationId="BtnScan" AutomationProperties.Name="扫描可清理项" Click="BtnScan_Click"/>
        <Button x:Name="BtnRecommend" Content="一键推荐" Width="94" Margin="8,0,0,0" Click="BtnRecommend_Click"/>
        <Button x:Name="BtnSelectNone" Content="全不选" Width="78" Margin="8,0,0,0" Click="BtnSelectNone_Click"/>
        <Button x:Name="BtnClean" Content="清理选中项…" Width="134" Margin="26,0,0,0" IsEnabled="False"
                AutomationProperties.AutomationId="BtnClean" AutomationProperties.Name="清理选中项" Click="BtnClean_Click"/>
      </StackPanel>
      <TextBlock x:Name="TxtScanInfo" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource MutedText}"/>
    </Grid>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="420"/>
      </Grid.ColumnDefinitions>

      <ListView x:Name="LvCategories" Style="{StaticResource TablesListView}" SelectionMode="Single"
                AutomationProperties.AutomationId="LvCategories" SelectionChanged="LvCategories_SelectionChanged">
        <ListView.View>
          <GridView>
            <GridViewColumn Width="42">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="类别" Width="180" DisplayMemberBinding="{Binding Name}"/>
            <GridViewColumn Header="说明" Width="352" DisplayMemberBinding="{Binding NoteText}"/>
            <GridViewColumn Header="风险" Width="78">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{Binding RiskBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding RiskText}" VerticalAlignment="Center"/>
                  </StackPanel>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="文件数" Width="86" DisplayMemberBinding="{Binding FilesText}"/>
            <GridViewColumn Header="大小" Width="96" DisplayMemberBinding="{Binding SizeText}"/>
            <GridViewColumn Header="建议" Width="62" DisplayMemberBinding="{Binding RecommendText}"/>
          </GridView>
        </ListView.View>
      </ListView>

      <Border Grid.Column="1" Margin="10,0,0,0" Background="{StaticResource SurfaceBrush}"
              BorderBrush="{StaticResource EdgeBrush}" BorderThickness="1" CornerRadius="4">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <StackPanel Margin="12,10,12,6">
            <TextBlock x:Name="TxtDetailTitle" Style="{StaticResource SectionText}" Text="选择左侧类别查看详情"/>
            <TextBlock x:Name="TxtDetailNote" Style="{StaticResource MutedText}" TextWrapping="Wrap"/>
          </StackPanel>
          <ListView Grid.Row="1" x:Name="LvDetailFiles" BorderThickness="0" Background="Transparent"
                    VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                    AutomationProperties.AutomationId="LvDetailFiles">
            <ListView.View>
              <GridView>
                <GridViewColumn Width="38">
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <CheckBox IsChecked="{Binding IsIncluded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="大小" Width="86" DisplayMemberBinding="{Binding SizeText}"/>
                <GridViewColumn Header="修改时间" Width="118" DisplayMemberBinding="{Binding DateText}"/>
                <GridViewColumn Header="路径" Width="160" DisplayMemberBinding="{Binding FullPath}"/>
              </GridView>
            </ListView.View>
          </ListView>
          <TextBlock Grid.Row="2" x:Name="TxtDetailMeta" Margin="12,6,12,10" Style="{StaticResource MutedText}" TextWrapping="Wrap"/>
        </Grid>
      </Border>
    </Grid>

    <Border Grid.Row="2" Margin="0,8,0,0" Background="{StaticResource SurfaceAltBrush}"
            BorderBrush="{StaticResource EdgeBrush}" BorderThickness="1" CornerRadius="4" Padding="10,6" Height="92">
      <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
        <TextBlock x:Name="TxtLog" FontFamily="Consolas, Microsoft YaHei UI" FontSize="11.5"
                   Foreground="{StaticResource Text2Brush}" TextWrapping="Wrap"/>
      </ScrollViewer>
    </Border>
  </Grid>
</UserControl>
