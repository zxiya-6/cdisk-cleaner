# Linux 版验收记录

验收日期：2026-09-16
验收环境：WSL2（CleanMaster-Ubuntu，Ubuntu 24.04）+ WSLg（X11 显示）
宿主：Windows 11 x64（Dell 15.7GB 内存）
仓库：zxiya-6/cdisk-cleaner（linux/ 子目录）

## 一、验收标准与结果总览

| # | 验收项 | 标准 | 结果 |
|---|---|---|---|
| 1 | 解决方案构建 | 0 警告 0 错误 | ✅ 通过 |
| 2 | 内置自检 | 9/9 全绿 | ✅ 通过 |
| 3 | CLI 端到端（沙箱） | e2e 6 组全过 | ✅ 通过 |
| 4 | GUI 交互（headless） | 5/5 全过 | ✅ 通过 |
| 5 | GUI 真实渲染（WSLg） | 窗口渲染正常 | ✅ 通过（见截图） |
| 6 | .deb 安装/卸载 | 安装→CLI→GUI→卸载 | ✅ 通过 |
| 7 | .rpm 内容与可执行 | 元数据+解包+CLI | ✅ 通过 |
| 8 | Arch .pkg.tar.zst | 读取+内容+CLI | ✅ 通过 |
| 9 | AppImage | 解包+CLI+自检 | ✅ 通过 |
| 10 | 清理-备份-恢复往返 | 数据可恢复 | ✅ 通过 |
| 11 | 安全保护 | Documents 等受保护目录不被触碰 | ✅ 通过 |

## 二、测试明细

### 1. 构建

```
dotnet build（解决方案 4 项目）→ 已成功生成，0 个警告，0 个错误
```

### 2. 内置自检（selftest）9 项

安全过滤器（11 拒绝 / 6 放行）、规则库校验（28 类）、样本扫描（150 文件精确匹配 +
符号链接跳过）、权限/长路径删除（374 字符）、回收站往返（.trashinfo）、备份区往返、
台账读写导出、取消响应（121ms）——全部通过。

### 3. CLI e2e（tests/e2e.sh，沙箱 /tmp/cc5-e2e，自动清理）

| 组 | 内容 | 结果 |
|---|---|---|
| 1 | 扫描计数（含 minAge 30 天过滤：旧安装包命中、近期包保留） | ✅ |
| 2 | 真实清理（Delete 策略） | ✅ |
| 3 | 回收站策略（移入 + .trashinfo + 近期文件保留） | ✅ |
| 4 | 备份区往返（移入→列出→恢复） | ✅ |
| 5 | 台账（记录 + CSV 导出） | ✅ |
| 6 | 安全保护（Documents 扫描为 0、文件完好） | ✅ |

### 4. GUI Headless（CleanMaster.App.HeadlessTests）

窗口 6 页签渲染、扫描点击产生类别列表、全选推荐/全不选、自检点击产生结果、
备份区/台账页签加载——5/5 通过。

### 5. GUI 真实渲染（WSLg + xwd 抓屏）

- 初始界面：`docs/gui-final.png`（构建版）
- .deb 安装后界面：`docs/gui-deb-installed.png`

顶部绿栏（版本/用户/可用空间）、6 页签、工具栏均正常渲染。

### 6. 安装包验证

| 包 | 验证内容 | 结果 |
|---|---|---|
| .deb（58.3MB） | apt install → PATH/desktop/图标 → version → selftest 9/9 → 清理→备份→恢复→台账 → GUI 运行与渲染 → apt remove | ✅ |
| .rpm（73.0MB） | rpm -qip 元数据（Name/Version/Architecture 正确）→ rpm2cpio 解包（usr/bin + desktop 完整）→ CLI version v1.0.0 + selftest 9/9 | ✅ |
| Arch .pkg.tar.zst（59.5MB） | bsdtar 读取 → .PKGINFO/.MTREE 完整 → 内容解包 → CLI version + selftest 9/9 | ✅ |
| AppImage（41.4MB） | --appimage-extract → CLI version v1.0.0 + selftest 9/9 | ✅ |

## 三、产物与校验和

产物目录：`installer/dist/`

| 文件 | 大小 | SHA256 |
|---|---|---|
| cleanmaster_1.0.0_amd64.deb | 58,340,574 | 734c4abd52b133cc74081a8acb6b35c71b4f04aec8e3afdcfab12fd2d70110bb |
| cleanmaster-1.0.0-1.x86_64.rpm | 72,984,778 | 3a4c041567c2279716d97b0e27bb40e9aebf1e027fecbb61bc1120492ae242cf |
| cleanmaster-1.0.0-1-x86_64.pkg.tar.zst | 59,467,126 | 1c9c17cfb3215640bb41f37cd2214063e3cd129a5e5353eafe50ba1a18b918fb |
| CleanMaster-1.0.0-x86_64.AppImage | 41,407,680 | 67757dee1d402f5353db8b4f83da94b3a850bc96778041c76448cf1932eb5de8 |

## 四、范围与未覆盖项

- 本机为 Ubuntu 24.04，.rpm / Arch 包通过元数据 + 解包 + 二进制运行验证，未在真实
  Fedora/Arch 系统上安装（WSL 环境限制）；包格式符合对应规范，依赖声明完整。
- 高风险清理项（Docker 缓存、旧内核、系统快照等）按需求默认不纳入。
- 未做跨架构（ARM64）构建；如需要，`-r linux-arm64` 重新 publish 即可。
- 规则在线更新需配置 `ruleUpdateUrl`，更新源预检（对照保护名单）逻辑已实现，未接线上源。
